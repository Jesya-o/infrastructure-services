---
- name: Install Nginx
  apt:
    name: nginx
    state: present

- name: Start Nginx Service
  systemd:
    name: nginx
    state: started

- name: Enable Nginx Service at Boot
  systemd:
    name: nginx
    enabled: yes

- name: Restart Nginx Service
  systemd:
    name: nginx
    state: restarted

- name: Copy Nginx configuration file
  copy:
    src: "{{ playbook_dir }}/roles/nginx/files/default"
    dest: "/etc/nginx/sites-enabled/default"
  notify: Restart Nginx
